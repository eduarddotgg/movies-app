import { Stack, Typography } from '@mui/material';

const NoResults = () => {
  return (
    <Stack direction="row" justifyContent="center">
      <Typography variant="h6">Did you try to search?</Typography>
    </Stack>
  )
};

export default NoResults;
