import { Stack, Typography } from '@mui/material';

const ErrorMessage = () => {
  return (
    <Stack direction="row" justifyContent="center">
      <Typography variant="h6">Error happened!</Typography>
    </Stack>
  )
};

export default ErrorMessage;
